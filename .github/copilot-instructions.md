# Copilot Instructions

Follow `AGENTS.md` as the main repository instruction file.

To avoid drift, keep long-lived standards only in `AGENTS.md`.

If there is a scope-specific extension file, apply it in addition to `AGENTS.md`.

